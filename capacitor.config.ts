import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobilePicData',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
